apiVersion: v1
kind: Service
metadata:
  name: mynginx-svc
  labels:
    app: mynginx
spec:
  type: NodePort
  selector:
    app: mynginx
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30000
    protocol: TCP
