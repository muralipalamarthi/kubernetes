apiVersion: v1
kind: Service
metadata:
  name: mynginx-svc
  labels:
    app: mynginx
    version: 2.6.1
spec:
  type: NodePort
  selector:
    app: mynginx
    version: 2.6.1
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
